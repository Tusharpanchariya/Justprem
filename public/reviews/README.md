The website plays the browser-compatible `review-1.mp4` through `review-7.mp4` files. The original MOV files are also retained here as source copies.

To replace one, keep its filename and overwrite the corresponding file.

The homepage loads these files in order. For the broadest browser support, future replacements should be MP4 files encoded with H.264 video and AAC audio.
